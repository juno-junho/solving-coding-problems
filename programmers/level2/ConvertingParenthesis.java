public class ConvertingParenthesis {
    public String solution(String p) {
        // p는 이미 균형잡힌 문자열 (와 )의 개수가 동일하다. 길이는 2이상 1000이하인 짝수다.

        // 1. 빈문자열 -> 빈문자열 반환
        if (p.length() == 0) return p;
        // 문자열 p를 균형잡힌 괄호 문자열 u와 v로 분리. u는 균형잡힌 괄호 문자열로 더이상 분리 할수 없어야 하며 v는 빈문자열 일 수 있다.

        // u가 올바른 괄호 문자열이면 v에 대해서 1단계 부터 다시 수행. 수행 결과 문자열을 u에 붙인 후 반환


        //


        String answer = "";
        return answer;
    }

    public static void main(String[] args) {

    }
}
